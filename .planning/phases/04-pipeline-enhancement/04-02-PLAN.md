---
phase: 04-pipeline-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - linkedin-posts/_pipeline/data/bundle-definitions.json
  - linkedin-posts/_pipeline/lib/bundle-curator.js
  - linkedin-posts/_pipeline/scripts/generate-bundles.js
autonomous: true
requirements:
  - AUDIT-01

must_haves:
  truths:
    - "BUNDLE_DEFINITIONS are loaded from data/bundle-definitions.json at runtime, not hardcoded in bundle-curator.js"
    - "Each bundle definition has a type field of either 'process' or 'technique'"
    - "bundle-curator.js validates the JSON schema on load and throws clear errors for malformed definitions"
    - "generate-bundles.js produces identical bundles.json output as before externalization (no regression)"
    - "Bundle definitions JSON supports a verifiedSteps field alongside idealizedSteps"
  artifacts:
    - path: "linkedin-posts/_pipeline/data/bundle-definitions.json"
      provides: "Externalized bundle definitions with type field"
      contains: "lead-capture-qualification"
    - path: "linkedin-posts/_pipeline/lib/bundle-curator.js"
      provides: "Bundle loader/validator reading from external JSON"
      contains: "bundle-definitions.json"
    - path: "linkedin-posts/_pipeline/scripts/generate-bundles.js"
      provides: "Bundle generation using externalized definitions"
  key_links:
    - from: "linkedin-posts/_pipeline/lib/bundle-curator.js"
      to: "linkedin-posts/_pipeline/data/bundle-definitions.json"
      via: "fs.readJsonSync or fs.readJSON"
      pattern: "bundle-definitions\\.json"
    - from: "linkedin-posts/_pipeline/scripts/generate-bundles.js"
      to: "linkedin-posts/_pipeline/lib/bundle-curator.js"
      via: "curateBundles(processes) or loadBundleDefinitions()"
      pattern: "curateBundles|loadBundleDefinitions"
---

<objective>
Externalize BUNDLE_DEFINITIONS from bundle-curator.js to a JSON data file with type field and verifiedSteps support.

Purpose: Enable per-YAML editing of bundle definitions without modifying code. The JSON file becomes the editable source of truth that audit phases (5-12) can update directly. Adding `type: 'process' | 'technique'` supports the new technique showcase post type. Adding optional `verifiedSteps` field prepares for audit-verified content injection.
Output: bundle-definitions.json data file, refactored bundle-curator.js as loader/validator, backward-compatible bundles.json output.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pipeline-enhancement/04-CONTEXT.md

<interfaces>
<!-- Current bundle-curator.js exports and bundle shape -->

From linkedin-posts/_pipeline/lib/bundle-curator.js:
```javascript
// Current: BUNDLE_DEFINITIONS hardcoded as const array (12 bundles)
// Current export:
module.exports = { curateBundles };

// Bundle definition shape (each item in BUNDLE_DEFINITIONS):
{
  id: 'lead-capture-qualification',
  title: 'Lead Capture & Qualification',
  journeyStage: 'Lead Capture',
  journeySlug: 'lead-capture',
  processIds: ['P-23', 'P-25', 'P-24', 'P-34'],
  idealizedSteps: [
    { label: '...', actor: '...' }
  ],
  pain: '...',
  solution: '...',
  layout: 'swimlane' | 'vertical'
}

// curateBundles(processes) returns enriched bundle objects with:
// id, title, journeyStage, journeySlug, processIds, constituentProcesses,
// idealizedSteps, pain, solution, inefficiencies, layout, sourceFileCount
```

From linkedin-posts/_pipeline/scripts/generate-bundles.js:
```javascript
const { curateBundles } = require('../lib/bundle-curator');
// ...
const bundles = curateBundles(processesData.processes);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract BUNDLE_DEFINITIONS to bundle-definitions.json and add type + verifiedSteps fields</name>
  <files>linkedin-posts/_pipeline/data/bundle-definitions.json</files>
  <read_first>
    linkedin-posts/_pipeline/lib/bundle-curator.js
  </read_first>
  <action>
Create `linkedin-posts/_pipeline/data/bundle-definitions.json` containing a JSON object with:
- `version`: "1.0"
- `generatedFrom`: "Extracted from bundle-curator.js BUNDLE_DEFINITIONS"
- `definitions`: array containing all 12 bundle definitions from the current BUNDLE_DEFINITIONS const in bundle-curator.js

Each definition object has the exact same fields as the current hardcoded objects, PLUS these new fields:
- `type`: "process" for all 12 existing bundles (they are all process bundles)
- `verifiedSteps`: null (placeholder for audit-verified step replacements per D-11)

The exact 12 bundles to extract (in order):
1. `lead-capture-qualification` - type: "process"
2. `appointment-booking-scheduling` - type: "process"
3. `estimate-to-deal-pipeline` - type: "process"
4. `change-order-management` - type: "process"
5. `contract-lifecycle` - type: "process"
6. `deal-to-job-processing` - type: "process"
7. `production-crew-tracking` - type: "process"
8. `time-tracking-sync-engine` - type: "process"
9. `invoice-lifecycle` - type: "process"
10. `expense-management-pipeline` - type: "process"
11. `reporting-dashboard-sync` - type: "process"
12. `communication-hub` - type: "process"

Copy ALL field values exactly from the current BUNDLE_DEFINITIONS array -- every idealizedStep label and actor, every pain/solution string, every processId, every layout value. Do not abbreviate, summarize, or modify any content.
  </action>
  <verify>
    <automated>node -e "const d=require('./linkedin-posts/_pipeline/data/bundle-definitions.json'); console.log('count:', d.definitions.length, 'types:', [...new Set(d.definitions.map(b=>b.type))].join(','), 'hasVerifiedSteps:', d.definitions.every(b=>'verifiedSteps' in b))"</automated>
  </verify>
  <acceptance_criteria>
    - bundle-definitions.json exists at linkedin-posts/_pipeline/data/bundle-definitions.json
    - JSON has `version` field set to "1.0"
    - JSON has `definitions` array with exactly 12 entries
    - Every definition has `type` field set to "process"
    - Every definition has `verifiedSteps` field set to null
    - Every definition has `id`, `title`, `journeyStage`, `journeySlug`, `processIds`, `idealizedSteps`, `pain`, `solution`, `layout` fields
    - First definition id is "lead-capture-qualification"
    - Last definition id is "communication-hub"
    - lead-capture-qualification has processIds ["P-23", "P-25", "P-24", "P-34"]
    - lead-capture-qualification has 8 idealizedSteps
    - communication-hub has processIds ["P-67", "P-53", "P-55", "P-47", "P-56"]
  </acceptance_criteria>
  <done>bundle-definitions.json contains all 12 bundle definitions extracted from bundle-curator.js with type and verifiedSteps fields added.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor bundle-curator.js to load from JSON and validate, update generate-bundles.js</name>
  <files>linkedin-posts/_pipeline/lib/bundle-curator.js, linkedin-posts/_pipeline/scripts/generate-bundles.js</files>
  <read_first>
    linkedin-posts/_pipeline/lib/bundle-curator.js
    linkedin-posts/_pipeline/scripts/generate-bundles.js
    linkedin-posts/_pipeline/data/bundle-definitions.json
    linkedin-posts/_pipeline/data/bundles.json
  </read_first>
  <action>
1. In `linkedin-posts/_pipeline/lib/bundle-curator.js`:
   - Remove the hardcoded `BUNDLE_DEFINITIONS` const array entirely
   - Add a `loadBundleDefinitions()` function that:
     a. Reads `path.join(__dirname, '..', 'data', 'bundle-definitions.json')` using `fs.readJsonSync` (from fs-extra, already required)
     b. Validates the loaded JSON: checks `definitions` is an array, each definition has required fields (`id`, `title`, `journeyStage`, `journeySlug`, `processIds`, `idealizedSteps`, `pain`, `solution`, `layout`, `type`)
     c. For any missing required field, throws Error: `Invalid bundle definition "${def.id || 'unknown'}": missing field "${fieldName}"`
     d. Returns the `definitions` array
   - Modify `curateBundles(processes)` to call `loadBundleDefinitions()` internally instead of referencing the removed const
   - In the bundle enrichment (the `.map()` inside curateBundles), when building the return object:
     - If `def.verifiedSteps` is a non-null array, use `def.verifiedSteps` as the steps in the output; otherwise use `def.idealizedSteps` (per D-11: verifiedSteps replaces idealizedSteps when populated)
     - Add `type: def.type` to the output bundle object
   - Export: `module.exports = { curateBundles, loadBundleDefinitions }`

2. In `linkedin-posts/_pipeline/scripts/generate-bundles.js`:
   - No changes needed to the import or call pattern -- `curateBundles(processesData.processes)` still works because the definitions are loaded internally
   - But add the `type` field to the summary log line: change `${b.idealizedSteps.length} steps` to `${b.idealizedSteps.length} steps, ${b.type}` so the type is visible in output

3. Verify no regression: run `node scripts/generate-bundles.js` from the _pipeline directory and confirm it produces 12 bundles with the same data as before. The output bundles.json should have identical bundle ids, titles, step counts, and layouts.
  </action>
  <verify>
    <automated>cd "D:/Work/Boolean/home-service-os/linkedin-posts/_pipeline" && node scripts/generate-bundles.js 2>&1 | findstr /C:"Generated 12 bundles"</automated>
  </verify>
  <acceptance_criteria>
    - bundle-curator.js does NOT contain `const BUNDLE_DEFINITIONS =` (hardcoded array removed)
    - bundle-curator.js contains `function loadBundleDefinitions()`
    - bundle-curator.js contains `bundle-definitions.json` path reference
    - bundle-curator.js contains `Invalid bundle definition` validation error message
    - bundle-curator.js contains `def.verifiedSteps` conditional logic
    - bundle-curator.js contains `type: def.type` in the return object
    - bundle-curator.js exports include `loadBundleDefinitions`
    - generate-bundles.js summary log contains `b.type` reference
    - Running `node scripts/generate-bundles.js` produces "Generated 12 bundles" output
    - Output bundles.json has 12 bundles with all original ids present
    - Each bundle in bundles.json now has a `type` field set to "process"
  </acceptance_criteria>
  <done>bundle-curator.js loads definitions from JSON at runtime with validation. generate-bundles.js produces identical output (12 bundles) with new type field. Backward compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON file -> bundle-curator.js | bundle-definitions.json is read from local filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | T (Tampering) | bundle-definitions.json | accept | File is under version control and only modified by pipeline scripts or developer. No untrusted input path. Schema validation in loadBundleDefinitions catches malformed data. |
</threat_model>

<verification>
1. bundle-definitions.json has 12 definitions with type and verifiedSteps fields
2. bundle-curator.js loads from JSON (no hardcoded BUNDLE_DEFINITIONS)
3. generate-bundles.js produces identical 12-bundle output
4. Each bundle in output has type: "process"
</verification>

<success_criteria>
- BUNDLE_DEFINITIONS fully externalized to data/bundle-definitions.json
- bundle-curator.js is a loader/validator, not a definition store
- verifiedSteps field supported (null by default, used when populated)
- type field present on all bundles ('process' for existing, 'technique' for future)
- Zero regression in bundles.json output
</success_criteria>

<output>
After completion, create `.planning/phases/04-pipeline-enhancement/04-02-SUMMARY.md`
</output>
